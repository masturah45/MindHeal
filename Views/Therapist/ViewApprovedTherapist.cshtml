@model IEnumerable<MindHeal.Models.DTOs.TherapistDto>
<partial name="_Notification" />

<div class="container p-3">

    <div class="row pt-4">

        <div class="col-6">

            <h2 calss="text-primary">ViewAll Approved Therapist</h2>

        </div>

        <div>
            <table class="table table-bordered table-striped" style="width:100%">

                <thead>

                    <tr>

                        <th>
                            FirstName
                        </th>

                        <th>
                            LastName
                        </th>

                        <th>
                            PhoneNumber
                        </th>
                        <th>
                            RegNo
                        </th>
                        <th>
                            Credential
                        </th>
                        <th>
                            Certificate
                        </th>

                    </tr>

                </thead>

                @foreach (var therapist in Model)
                {
                    <tr>
                        <th>
                            @therapist.FirstName
                        </th>

                        <th>
                            @therapist.LastName
                        </th>

                        <th>
                            @therapist.PhoneNumber
                        </th>
                        <th>
                            @therapist.RegNo
                        </th>
                        <th>
                            @therapist.Credential
                        </th>
                        <th>
                            @therapist.Certificate
                        </th>
                    </tr>
                }

            </table>
        </div>

    </div>
</div>