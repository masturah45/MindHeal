@*@model IEnumerable<MindHeal.Models.DTOs.TherapistDto>
@using Microsoft.AspNetCore.Identity;
@using MindHeal.Models.Entities;
@using System.Security.Claims;
@{

    var role = User.FindFirstValue(ClaimTypes.Role);
}
<partial name="_Notification" />

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">UI Elements /</span> Cards Basic</h4>

    if(@role == Client)
    {
    <div class="container-xxl flex-grow-1 container-p-y">
        <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">UI Elements /</span> Cards Basic</h4>



        <h5 class="pb-1 mb-4">All Therapist</h5>
            <div class="row mb-5">
            @foreach (var therapist in Model)
            {
                <div class="col-md-6 col-xl-4">
                    <div class="card mb-3">
                        <img class="card-img-top" src="~/Documents/@therapist.ProfilePicture" alt="Card image cap" height="300" />
                        <div class="card-body">
                            <h5 class="card-title">@therapist.FirstName @therapist.LastName </h5>
                            <h5 class="card-title">
                                @foreach (var item in therapist.Issues)
                                {
                                    <span>  @item.Name ,  </span>
                                }
                            </h5>
                            <p class="card-text">
                                @therapist.RegNo @therapist.PhoneNumber
                            </p>
                            <p class="card-text">
                                <small class="text-muted">@therapist.Description </small>
                            </p>
                            <th>
                                <a asp-controller="Chat" asp-action="Chat" asp-route-therapistId="@therapist.Id" class="btn btn-warning"><i class="bi bi-pencil-fill"></i>Chat With Therapist</a>
                            </th>
                        </div>
                    </div>
                </div>
       }
        <!--/ Images -->
    </div>
    <div class="content-backdrop fade"></div>
    }
*@
<marquee>
    <h1 class="d-flex align-items-center display-1 justify-content-center text-center text-primary">Welcome to Mind Heal</h1>
</marquee>